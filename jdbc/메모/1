VO. DAO 패턴
VO(value Object)
        -데이터(정보)를 표현하는 객체다.
        -보통은 특정 테이블의 한 행을 표현한다.
        -테이블당 하나의 VO 클래스가 필요하다.
DAO(Date Access Object)
        -데이터베이스 엑세르를 담당하는 객체다
        -보통은 특정 테이블마다 하나의 DAO 클래스가 필요하다.
        -DAO 클래스에는 특정 테이블에 대한 CURD(추가,조회,변경,삭제) 작업이 구현되어 있다.
